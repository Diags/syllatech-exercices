[project]
name = "jobportal-cc-sandbox"
version = "1.0.0"
description = "Projet de depart du cours syllatech « Sandbox Claude Code »"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["outils"]
