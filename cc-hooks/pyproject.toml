[project]
name = "jobportal-hooks"
version = "1.0.0"
description = "Projet de depart du cours syllatech « Claude Code : les Hooks en pratique »"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8", "ruff>=0.9"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = [".claude"]
