# Les valeurs que `compose.yaml` interpole avec ${...}.
# ⚠️ Valeurs de demonstration : ce fichier est LU par le cours, il ne
# protege rien. Un vrai `.env` ne se versionne pas.
DB_PASSWORD=mot-de-passe-factice-du-cours
DB_NOM=jobportal
