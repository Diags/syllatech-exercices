"""Docker et Kubernetes, ecrits en clair.

Aucun demon Docker, aucun cluster. Ce que ce paquet contient, ce sont les
*algorithmes* : le cache de couches d'un `docker build`, le systeme de
fichiers empile d'une image, l'ordre de demarrage d'un Compose, et la
boucle de reconciliation d'un cluster Kubernetes.

Les chapitres impriment ce que ce code produit sur de vrais `Dockerfile`,
`compose.yaml` et manifestes `.yaml`.
"""
