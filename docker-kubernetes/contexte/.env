# ⚠️ PIECE A CONVICTION — fichier de demonstration du cours.
# Aucune valeur reelle ici : c'est un leurre, et il est volontairement
# present pour que le chapitre 2 montre qu'un `COPY . .` l'emporte.
DB_PASSWORD=mot-de-passe-factice-du-cours
API_TOKEN=jeton-factice-du-cours
